# Server-side translation API credentials (never expose in frontend code)
GOOGLE_TRANSLATE_API_KEY=your_google_translate_api_key_here
PORT=3001

# Optional frontend override for API base URL (leave empty in development to use Vite proxy)
VITE_API_BASE_URL=
